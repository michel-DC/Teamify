{
  "buildCommand": "prisma generate && next build",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "env": {
    "NODE_ENV": "production"
  },
  "ignoredBuildStep": "if [ \"$VERCEL_GIT_BRANCH\" = \"non-stable\" ] || [ \"$VERCEL_GIT_BRANCH\" = \"stable\" ]; then exit 0; fi",
  "functions": {
    "src/app/api/**/*.ts": {
      "maxDuration": 30
    }
  },
  "regions": ["iad1"],
  "build": {
    "env": {
      "VERCEL_LOG_LEVEL": "info"
    }
  }
}
